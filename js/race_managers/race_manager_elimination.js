'use strict';

class RaceManagerElimination {
	constructor() {
		super();
		this.mancheCount = 1;
	}

	// mancheList deve avere una sola manche
	// manche2 viene formata dai non vincitori di manche1
	// manche3 viene formata da vincitori manche1 + vincitori manche2
	// manche4+ viene formata dai vincitori della precedente
}

module.exports = RaceManagerElimination;
